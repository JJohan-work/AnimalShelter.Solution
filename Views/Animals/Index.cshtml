@{
  Layout = "_Layout";
}

@using AnimalShelter.Models;

<h1>Animals</h1>

<p>@Html.ActionLink("Sort By Default", "Index", new {userInput = "Default"})</p>
<p>@Html.ActionLink("Sort By Type", "Index", new {userInput = "EnglishType"})</p>
<p>@Html.ActionLink("Sort By Name", "Index", new {userInput = "Name"})</p>


  @* @using (Html.BeginForm("IndexSearch")){

  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  <input type="submit" value="Filter"/>
  } *@

@if (Model.Count == 0)
{
  <h3>No animals have been added yet.</h3>
}

<ul>
  @foreach (Animal animal in Model)
  {
    <li>@Html.ActionLink($"{animal.Name}", "Details", new { id = animal.AnimalId })</li>
  }
</ul>

<p>@Html.ActionLink("Add new animal", "Create")</p>